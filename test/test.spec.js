"use strict";
const expect = require("chai").expect;
const assert = require("chai").assert;
const cookie = require("../lib/index").cookie;

describe('testing cookie() function to safely set cookie values', function()  {

	it('should have cookie named testCookie', function() {

	});

});
